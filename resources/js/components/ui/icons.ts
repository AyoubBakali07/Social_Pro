import { Facebook, Instagram, Linkedin, Twitter, FileText, X, Loader2 } from 'lucide-vue-next';

export const iconMap: Record<string, any> = {
  Facebook,
  Instagram,
  Linkedin,
  Twitter,
  FileText,
  X,
  Loader2,
  // Add more icons as needed
}; 